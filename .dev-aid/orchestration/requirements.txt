# Dev-AID Router - Python Dependencies
# Install with: pip install -r requirements.txt
#
# SECURITY: All dependencies are pinned to exact versions for reproducibility.
# This prevents supply chain attacks and ensures deterministic builds.
#
# To update dependencies:
#   1. Test new versions in isolated environment
#   2. Update pins below after verification
#   3. Run: pip install -r requirements.txt
#   4. Test: pytest .dev-aid/orchestration/tests/

# AI Provider SDKs
anthropic==0.75.0            # Anthropic API (Claude) - Updated for httpx 0.28+ compatibility
google-genai==1.53.0         # Google Gemini API (Unified SDK)
openai==1.54.5               # OpenAI API

# Core utilities
python-dotenv==1.0.1        # Load .env files for API keys
keyring==25.7.0             # Access system keychain for session tokens (Claude Code, etc.)
requests==2.33.1            # HTTP requests (fallback) - bumped for CVE-2026-25645 (extract_zipped_paths temp file reuse)
pydantic==2.12.5            # Data validation and settings - 2.12+ required for Python 3.14 wheels (pydantic-core 2.45+)
email-validator==2.3.0      # Email validation for Pydantic
psutil==6.1.1               # Cross-platform hardware detection (CPU, RAM, GPU)

# CLI and formatting
rich==14.2.0                # Pretty terminal output
typer==0.20.0               # CLI framework

# Token optimization
toon-format==0.9.0b1        # TOON encoder/decoder - 30-60% token reduction vs JSON

# HTTP client
httpx==0.28.1                # Modern HTTP client (used by Anthropic, Google, OpenAI SDKs)

# Testing dependencies
pytest==8.3.5               # Testing framework
pytest-asyncio==0.24.0      # Async test support
pytest-cov==5.0.0           # Coverage reporting
pytest-mock==3.14.0         # Mocking support
pytest-xdist==3.5.0         # Parallel testing support
vcrpy==8.1.1                # Record/replay HTTP interactions for testing - 8.1+ required for Python 3.14 (fixes iscoroutinefunction deprecation)
hypothesis==6.113.0          # Property-based testing

# Security & Quality
bandit==1.7.10              # Security vulnerability scanner
safety==3.6.2               # Dependency vulnerability scanner
pip-audit==2.7.3             # Audit pip packages for vulnerabilities
mypy==1.14.1                # Static type checker
# types-requests temporarily removed due to urllib3 conflict with vcrpy on Python 3.8
# types-requests==2.31.0.20240406  # Type stubs for requests

# Development tools
pre-commit==3.5.0            # Git hooks framework
black==26.3.1                # Code formatter - bumped for CVE-2026-32274 (arbitrary file write via cache filename)
isort==5.13.2                # Import sorter
flake8==7.1.2                # Linting

# Note: Transitive dependencies are automatically resolved by pip
# Only direct dependencies are pinned above to avoid version conflicts

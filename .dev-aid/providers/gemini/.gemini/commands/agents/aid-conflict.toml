name = "aid-conflict"
description = "Short alias for dev-aid-agent-conflict-resolve — merge conflict resolution"

[metadata]
category = "agents"
author = "Dev-AID Team"
version = "1.0.0"

[prompt]
content = """
# Conflict Resolution (Short Alias)

This is a short alias for the full `dev-aid-agent-conflict-resolve` command.

**Read and follow all instructions from:** `.dev-aid/providers/gemini/.gemini/commands/agents/dev-aid-agent-conflict-resolve.toml`

Load that file now and execute the conflict resolution workflow. The user's input is the PR number and strategy.
"""

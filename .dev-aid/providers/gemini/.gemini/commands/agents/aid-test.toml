name = "aid-test"
description = "Generate tests for untested code paths"

[metadata]
category = "agents"
author = "Dev-AID Team (https://probably.group)"
version = "1.0.0"

[prompt]
content = """
# Test Generation (Short Alias)

This is a short alias for the full `dev-aid-agent-test-gen` command.

**Read and follow all instructions from:** `.dev-aid/providers/gemini/.gemini/commands/agents/dev-aid-agent-test-gen.toml`

Load that file now and execute the test generation workflow. The user's input is the target path.
"""

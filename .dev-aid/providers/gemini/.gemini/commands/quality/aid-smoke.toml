[metadata]
name = "aid-smoke"
description = "Run smoke tests (alias for dev-aid-smoke)"
category = "quality"
author = "Dev-AID"
version = "1.0.0"

[prompt]
text = """
Use the full command instructions from `.dev-aid/providers/gemini/.gemini/commands/quality/dev-aid-smoke.toml` with these arguments: $ARGUMENTS
"""

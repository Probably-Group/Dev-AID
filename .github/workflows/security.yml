name: Dev-AID Security Scan

# Comprehensive security scan for releases, version tags, and scheduled maintenance
# Runs on: releases, version tags, manual trigger, and weekly schedule
# For PR checks, see pr-check.yml (faster, lighter validation)
# Tools: Gitleaks (secrets), Trivy (CVE + misconfig + secrets), Opengrep (SAST 340+ rules)
on:
  push:
    tags:
      - 'v*'  # Trigger on version tags (v1.0.0, v2.1.3, etc.)
    # Only run on main/master when security-critical files change
    branches: [main, master]
    paths:
      - '.github/workflows/security.yml'
      - '**/requirements.txt'
      - '**/package.json'
      - '**/package-lock.json'
      - '**/Gemfile'
      - '**/Gemfile.lock'
      - '**/go.mod'
      - '**/go.sum'
      - 'Dockerfile'
      - '.dockerignore'
      - '.dev-aid/config/**'
  release:
    types: [published, created]
  # Allow manual triggers
  workflow_dispatch:
  # Weekly scheduled scan (Monday at 2 AM UTC)
  schedule:
    - cron: '0 2 * * 1'

jobs:
  security-scan:
    name: Comprehensive Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # security-events: write # Disabled for private repos without Advanced Security
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Opengrep
        run: |
          # Install Opengrep (open-source fork of Semgrep OSS) via official method - latest
          curl -fsSL https://raw.githubusercontent.com/opengrep/opengrep/main/install.sh | bash
          # The installer places the binary at ~/.opengrep/cli/latest/opengrep
          # (the symlink target). Add that directory — and the legacy ~/.local/bin —
          # to PATH so subsequent steps can invoke `opengrep`.
          echo "$HOME/.opengrep/cli/latest" >> "$GITHUB_PATH"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          # Verify install for fast-fail diagnostics
          "$HOME/.opengrep/cli/latest/opengrep" --version

      - name: Install Gitleaks
        run: |
          # gitleaks-action@v2 requires a paid license for organizations.
          # Install the gitleaks binary directly from GitHub releases instead
          # (the binary itself is OSS / MIT-licensed; only the GH Action wrapper is gated).
          GITLEAKS_VERSION=8.30.1
          curl -sSfL \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            -o /tmp/gitleaks.tar.gz
          tar -xzf /tmp/gitleaks.tar.gz -C /tmp gitleaks
          sudo mv /tmp/gitleaks /usr/local/bin/gitleaks
          gitleaks version

      - name: Run Gitleaks (Secret Scanning)
        id: gitleaks
        continue-on-error: true
        run: |
          # `detect` scans the working tree + git history. Exit code 0 = no leaks,
          # 1 = leaks found, 2 = error. We capture findings in gitleaks-report.json
          # and let the dedicated check step decide whether to fail the build.
          gitleaks detect \
            --source=. \
            --report-format=json \
            --report-path=gitleaks-report.json \
            --redact \
            --no-banner \
            --exit-code=0

      - name: Run Opengrep (SAST - 340+ rules)
        id: opengrep
        continue-on-error: true
        run: |
          # Comprehensive rulesets (340+ rules):
          # - p/default: Semgrep curated defaults
          # - p/security-audit: Comprehensive security patterns
          # - p/secrets: Hardcoded credentials
          # - p/ci: CI/CD security (GitHub Actions)
          # - p/cwe-top-25: MITRE CWE Top 25
          opengrep \
            --config=p/default \
            --config=p/security-audit \
            --config=p/secrets \
            --config=p/ci \
            --config=p/cwe-top-25 \
            --json --output=opengrep.json .

      - name: Run Trivy (CVE + Misconfig + Secrets)
        uses: aquasecurity/trivy-action@v0.35.0
        continue-on-error: true
        with:
          version: latest
          scan-type: 'fs'
          scan-ref: '.'
          scanners: 'vuln,misconfig,secret'  # Comprehensive: CVEs + Dockerfiles/IaC + secrets
          format: 'json'
          output: 'trivy-fs.json'
          severity: 'HIGH,CRITICAL'

      - name: Build Docker Image
        if: hashFiles('Dockerfile') != ''
        continue-on-error: true
        run: docker build -t temp-image:latest .

      - name: Run Trivy (Container Scanning)
        if: hashFiles('Dockerfile') != ''
        uses: aquasecurity/trivy-action@v0.35.0
        continue-on-error: true
        with:
          version: latest
          scan-type: 'image'
          image-ref: 'temp-image:latest'
          scanners: 'vuln,misconfig,secret'
          format: 'json'
          output: 'trivy-image.json'
          severity: 'HIGH,CRITICAL'

      # Disabled SARIF upload for private repo compatibility
      # - name: Upload SARIF results
      #   uses: github/codeql-action/upload-sarif@v3
      #   ...

      - name: Upload security reports as artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-reports
          path: |
            *-report.json
            *.json
          retention-days: 30

      - name: Generate Security Summary
        if: always()
        run: |
          echo "# 🔒 Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Gitleaks
          if [ -f "gitleaks-report.json" ]; then
            LEAKS=$(jq 'length' gitleaks-report.json)
            if [ "$LEAKS" -gt 0 ]; then
              echo "## ❌ Gitleaks: $LEAKS secret(s) found" >> $GITHUB_STEP_SUMMARY
            else
              echo "## ✅ Gitleaks: No secrets found" >> $GITHUB_STEP_SUMMARY
            fi
          fi

          # Trivy
          if [ -f "trivy-fs.json" ]; then
            VULNS=$(jq '[.Results[]?.Vulnerabilities[]?] | length' trivy-fs.json)
            MISCONFIGS=$(jq '[.Results[]?.Misconfigurations[]?] | length' trivy-fs.json)
            SECRETS=$(jq '[.Results[]?.Secrets[]?] | length' trivy-fs.json)
            echo "## 🛡️ Trivy: $VULNS CVEs, $MISCONFIGS misconfigs, $SECRETS secrets" >> $GITHUB_STEP_SUMMARY
          fi

          # Opengrep
          if [ -f "opengrep.json" ]; then
            FINDINGS=$(jq '.results | length' opengrep.json)
            echo "## 🔍 Opengrep: $FINDINGS finding(s) (340+ rules)" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Check for critical issues
        if: always()
        run: |
          CRITICAL_ISSUES=0

          # Check Gitleaks - check if results file exists and contains findings
          if [ -f "gitleaks-report.json" ]; then
            LEAKS=$(jq 'length' gitleaks-report.json)
            if [ "$LEAKS" -gt 0 ]; then
              echo "❌ $LEAKS secret(s) found!"
              CRITICAL_ISSUES=$((CRITICAL_ISSUES + 1))
            fi
          elif [ "${{ steps.gitleaks.outcome }}" = "failure" ]; then
            echo "❌ Gitleaks scan failed!"
            CRITICAL_ISSUES=$((CRITICAL_ISSUES + 1))
          fi

          # Check Trivy for CRITICAL CVEs and secrets
          if [ -f "trivy-fs.json" ]; then
            CRITICAL_CVE=$(jq -r '[.Results[]?.Vulnerabilities[]? | select(.Severity == "CRITICAL")] | length' trivy-fs.json)
            CRITICAL_MISCONFIG=$(jq -r '[.Results[]?.Misconfigurations[]? | select(.Severity == "CRITICAL")] | length' trivy-fs.json)
            SECRETS=$(jq -r '[.Results[]?.Secrets[]?] | length' trivy-fs.json)

            if [ "$CRITICAL_CVE" -gt 0 ]; then
              echo "❌ $CRITICAL_CVE critical CVEs!"
              CRITICAL_ISSUES=$((CRITICAL_ISSUES + 1))
            fi
            if [ "$CRITICAL_MISCONFIG" -gt 0 ]; then
              echo "❌ $CRITICAL_MISCONFIG critical misconfigurations!"
              CRITICAL_ISSUES=$((CRITICAL_ISSUES + 1))
            fi
            if [ "$SECRETS" -gt 0 ]; then
              echo "❌ $SECRETS secrets found in files!"
              CRITICAL_ISSUES=$((CRITICAL_ISSUES + 1))
            fi
          fi

          # Check Opengrep for ERROR severity
          if [ -f "opengrep.json" ]; then
            CRITICAL=$(jq -r '[.results[] | select(.extra.severity == "ERROR")] | length' opengrep.json)
            if [ "$CRITICAL" -gt 0 ]; then
              echo "❌ $CRITICAL critical code vulnerabilities!"
              CRITICAL_ISSUES=$((CRITICAL_ISSUES + 1))
            fi
          fi

          if [ $CRITICAL_ISSUES -gt 0 ]; then
            echo ""
            echo "🚨 $CRITICAL_ISSUES critical security issue(s) found!"
            echo "Review the security reports and fix before merging."
            exit 1
          else
            echo "✅ No critical security issues found!"
          fi

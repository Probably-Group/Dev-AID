# .gitleaksignore - Fingerprint-based exclusions for Gitleaks
#
# NOTE: Path-based exclusions are now in .gitleaks.toml
# This file is for fingerprint-based exclusions (specific findings to ignore)
#
# Format: Each line should be a finding fingerprint (SHA hash)
# Get fingerprints by running: gitleaks detect --report-format json
# and extracting the "Fingerprint" field from findings.
#
# See: https://github.com/gitleaks/gitleaks#gitleaksignore

# Add fingerprints here if needed for specific false positives
# that can't be excluded by path patterns in .gitleaks.toml
